# 简历项目描述

## 海面哨兵：基于 YOLO26 的船只实时监测与告警系统

**Python | YOLO26 | PyTorch | OpenCV | ByteTrack | PySide6**

**模型与跟踪：** 基于 Ultralytics YOLO26 训练船只检测模型，封装图片、视频与摄像头三类推理入口；结合 ByteTrack 完成多目标持续跟踪，并设计可回收显示 ID 映射，避免长时间监控中目标编号持续膨胀。

**实时监测：** 基于 PySide6 与 QThread 构建桌面端监控界面，将视频解码、模型推理与 UI 渲染解耦；支持置信度及 IoU 阈值动态配置，实时展示目标数量、最高置信度和推理 FPS，并兼容自训练单类权重与 COCO 多类模型。

**告警与工程化：** 实现船只进入/离开状态机，触发声光告警并将时间、来源、目标数和置信度写入 CSV 事件日志；支持一键保存检测截图，补充参数化训练脚本、依赖锁定与核心逻辑单元测试，形成可复现的训练、推理和验证流程。
